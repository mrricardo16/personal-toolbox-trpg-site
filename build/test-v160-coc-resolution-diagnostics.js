"use strict";
const fs=require("fs"),path=require("path"),vm=require("vm"),assert=require("assert"),{webcrypto}=require("crypto"),{TextEncoder,TextDecoder}=require("util");
const root=path.resolve(__dirname,"..");
const files=["scenarios/library.js","state.js","check-engine.js","scenario-engine.js","case-integrity.js","memory.js","ai-protocol.js","player-action-guard.js","interaction-availability.js","saves.js","api-response-resilience.js","progress-semantics.js","authored-threat-clock.js","npc-knowledge-boundary.js","ending-resolution-gate.js","coc-resolution-engine.js"];
function storage(){const map=new Map();return{getItem:k=>map.has(String(k))?map.get(String(k)):null,setItem:(k,v)=>map.set(String(k),String(v)),removeItem:k=>map.delete(String(k)),clear:()=>map.clear(),key:i=>Array.from(map.keys())[i]??null,get length(){return map.size}}}
function node(){return{className:"",textContent:"",innerHTML:"",value:"",checked:false,disabled:false,style:{},dataset:{},classList:{add(){},remove(){},toggle(){}},appendChild(){},remove(){},click(){},insertAdjacentHTML(){},setAttribute(){},removeAttribute(){},addEventListener(){},querySelector(){return null},querySelectorAll(){return[]},scrollHeight:0,scrollTop:0}}
const localStorage=storage(),sessionStorage=storage(),sandbox={Object,Array,JSON,Map,Set,console,crypto:webcrypto,TextEncoder,TextDecoder,URL,AbortController,Blob,structuredClone,fetch:async()=>{throw new Error("fetch not expected")},setTimeout:()=>0,clearTimeout(){},setInterval:()=>0,clearInterval(){},window:{localStorage,sessionStorage,addEventListener(){}},document:{addEventListener(){},querySelector(){return null},querySelectorAll(){return[]},createElement:node,body:{appendChild(){}}},navigator:{clipboard:{writeText:async()=>{}}},confirm:()=>false,alert(){},renderAll(){},renderTopbar(){},renderSidebar(){},renderChat(){},renderChatLog(){},renderChatComposer(){},renderSaves(){},toast(){}};sandbox.globalThis=sandbox;
const source=files.map(file=>fs.readFileSync(path.join(root,"src",file),"utf8")).join("\n\n")+`\n;scheduleAutosave=()=>{};maybeAutoSummarize=()=>{};renderAll=()=>{};renderTopbar=()=>{};renderSidebar=()=>{};renderChat=()=>{};renderChatLog=()=>{};renderChatComposer=()=>{};renderSaves=()=>{};toast=()=>{};
function __character(){return{system:"coc7",name:"Diagnostic Investigator",hp:12,maxHp:12,san:60,maxSan:99,luck:50,attributes:{str:50,con:60,siz:60,dex:60,app:50,int:70,pow:60,edu:70},skills:[{id:"spot_hidden",name:"侦查",value:60}]}}
function __scenario(){return{id:"scenario-v160-diagnostic",title:"Diagnostic",mode:"structured",system:"coc7",metadata:{era:"1920s"},briefing:{subtitle:"test",playerRole:"调查员",knownFacts:[],caseObjectives:[],opening:"测试。"},director:{},initialLeads:[],initialQuestions:[],endings:[{id:"withdraw",title:"撤离",alwaysAvailable:true}],chapters:[{id:"c",title:"c",scenes:[{id:"s",title:"s",nodes:[{id:"n",title:"测试室",background:"",goals:[],clues:[],npcs:[],optionalChecks:[],mandatoryChecks:[],exits:[],keeperNotes:""}]}]}]}}
function __ready(){state=makeInitialState();state.character=__character();activateScenario(__scenario());return deepClone(state)}
function __record(visibility,total){const check=normalizeCheck({required:true,mandatory:true,visibility,trigger:"ai",origin:"ai",system:"coc7",type:"skill",skillId:"spot_hidden",label:"侦查",reason:"诊断",difficulty:"regular"});const rank=cocRank(total,60),roll={expression:"1d100",rawRolls:[total],modifier:0,total,target:60,difficulty:"regular",difficultyTarget:60,rank,result:cocDifficultyPass(rank,"regular")},record=makeCheckRecord(check,roll,false);state.checkRecords.push(record);return deepClone(record)}
function __diagnostic(includeSecrets=false){return deepClone(buildDiagnosticPackage({includeSecrets}))}
function __ensureTwice(record){const copy=deepClone(record);ensureCocOutcomeContract(copy);const first={resolution:copy.resolutionContract.contractId,outcome:copy.outcomeContract.contractId};ensureCocOutcomeContract(copy);return{first,second:{resolution:copy.resolutionContract.contractId,outcome:copy.outcomeContract.contractId},record:copy}}
function __revision(){return state.revision}
globalThis.__test={ready:__ready,record:__record,diagnostic:__diagnostic,ensureTwice:__ensureTwice,revision:__revision};`;
vm.createContext(sandbox);vm.runInContext(source,sandbox,{filename:"v160-coc-resolution-diagnostics-runtime.js"});
const api=sandbox.__test;let passed=0;function test(name,fn){fn();passed++;console.log(`PASS ${name}`)}
api.ready();const publicRecord=api.record("public",20),secretRecord=api.record("secret",80);
test("默认诊断只包含公开 CoC Outcome Contract",()=>{const d=api.diagnostic(false),ids=d.cocResolutionEngine.recentOutcomes.map(x=>x.recordId);assert(ids.includes(publicRecord.id));assert(!ids.includes(secretRecord.id));assert.equal(d.includeSecrets,false)});
test("显式 includeSecrets 才包含暗骰 Outcome Contract",()=>{const d=api.diagnostic(true),ids=d.cocResolutionEngine.recentOutcomes.map(x=>x.recordId);assert(ids.includes(publicRecord.id));assert(ids.includes(secretRecord.id));assert.equal(d.includeSecrets,true)});
test("重复 ensure 不会重新生成 Contract ID",()=>{const legacy=JSON.parse(JSON.stringify(publicRecord));delete legacy.resolutionContract;delete legacy.outcomeContract;const x=api.ensureTwice(legacy);assert.equal(x.first.resolution,x.second.resolution);assert.equal(x.first.outcome,x.second.outcome);assert.equal(x.first.resolution,x.first.outcome)});
test("读取默认诊断不会推进 canonical revision",()=>{const before=api.revision();api.diagnostic(false);assert.equal(api.revision(),before)});
console.log(`V160_COC_RESOLUTION_DIAGNOSTIC_TESTS:${passed}:PASS`);
