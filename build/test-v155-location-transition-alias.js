"use strict";
const fs=require("fs"),path=require("path"),vm=require("vm"),assert=require("assert"),{webcrypto}=require("crypto"),{TextEncoder,TextDecoder}=require("util");
const root=path.resolve(__dirname,"..");
const files=["scenarios/library.js","state.js","check-engine.js","scenario-engine.js","memory.js","ai-protocol.js","saves.js"];
function storage(){const map=new Map();return{getItem:k=>map.has(String(k))?map.get(String(k)):null,setItem:(k,v)=>map.set(String(k),String(v)),removeItem:k=>map.delete(String(k)),clear:()=>map.clear(),key:i=>Array.from(map.keys())[i]??null,get length(){return map.size}}}
const localStorage=storage(),sessionStorage=storage();
const sandbox={Object,Array,JSON,Map,Set,console,crypto:webcrypto,TextEncoder,TextDecoder,URL,AbortController,Blob,structuredClone,fetch:async()=>{throw new Error("fetch not expected")},setTimeout:()=>0,clearTimeout(){},setInterval:()=>0,clearInterval(){},window:{localStorage,sessionStorage,addEventListener(){}},document:{querySelector(){return null},querySelectorAll(){return[]},createElement(){return{className:"",textContent:"",style:{},classList:{add(){},remove(){},toggle(){}},appendChild(){},remove(){},click(){}}},body:{appendChild(){}}},confirm:()=>false,renderAll(){},renderTopbar(){},renderSidebar(){},renderChat(){},renderChatLog(){},renderChatComposer(){},renderSaves(){},toast(){}};
sandbox.globalThis=sandbox;
const source=files.map(file=>fs.readFileSync(path.join(root,"src",file),"utf8")).join("\n\n")+`\n;scheduleAutosave=()=>{};renderAll=()=>{};renderTopbar=()=>{};renderSidebar=()=>{};renderChat=()=>{};renderChatLog=()=>{};renderChatComposer=()=>{};renderSaves=()=>{};toast=()=>{};globalThis.__test={APP_VERSION,SCHEMA_VERSION,scenarioById:id=>deepClone(SCENARIO_LIBRARY.find(x=>x.id===id)),activateScenario,validateAiResponse,prepareAiTransaction,snapshot:()=>deepClone(state),reset:()=>{state=makeInitialState();state.character={system:"coc7",name:"E2E",hp:12,maxHp:12,san:65,maxSan:99,luck:50,attributes:{str:55,con:60,siz:60,dex:65,app:50,int:70,pow:65,edu:70},skills:[{id:"spot_hidden",name:"侦查",value:75}]};}};`;
vm.createContext(sandbox);vm.runInContext(source,sandbox,{filename:"v155-location-transition-runtime.js"});
const api=sandbox.__test;let passed=0;function test(name,fn){fn();passed++;console.log(`PASS ${name}`)}
function ready(){api.reset();api.activateScenario(api.scenarioById("scenario-old-house"));return api.snapshot()}
function rawMove(baseRevision,{type="transition",targetNodeId="old-servant-room",nodeProposal={id:"old-servant-room"}}={}){return{protocolVersion:"1.3",requestId:"r-move",baseRevision,decision:"no_check",narrative:"你离开大厅，进入管家房。",check:null,stateChanges:[],campaignChanges:[],locationEffect:{type,targetNodeId},nodeProposal,endingProposal:null,actionSuggestions:[]}}

test("版本为 v1.5.5",()=>assert.ok((()=>{const v=api.APP_VERSION.split(".").map(Number);return v[0]>1||v[0]===1&&(v[1]>5||v[1]===5&&v[2]>=5)})()));
test("Schema 保持 8",()=>assert.equal(api.SCHEMA_VERSION,8));
test("transition 安全归一为 transition_proposal",()=>{const s=ready(),parsed=api.validateAiResponse(rawMove(s.revision),{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.equal(parsed.locationEffect.type,"transition_proposal")});
test("transition + nodeProposal.id 可通过完整合法出口事务",()=>{const s=ready(),parsed=api.validateAiResponse(rawMove(s.revision),{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"}),tx=api.prepareAiTransaction(parsed);assert.equal(tx.proposal.targetNodeId,"old-servant-room")});
test("标准 transition_proposal 保持不变",()=>{const s=ready(),parsed=api.validateAiResponse(rawMove(s.revision,{type:"transition_proposal"}),{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.equal(parsed.locationEffect.type,"transition_proposal")});
test("stay 等既有枚举保持不变",()=>{const s=ready(),raw=rawMove(s.revision,{type:"stay",targetNodeId:null,nodeProposal:null});raw.narrative="你仍留在大厅，没有移动。";const parsed=api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.equal(parsed.locationEffect.type,"stay")});
test("未知地点枚举仍严格拒绝",()=>{const s=ready(),raw=rawMove(s.revision,{type:"teleport"});assert.throws(()=>api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"}),/地点|locationEffect|不支持/)});
test("transition 别名不会放行不存在节点",()=>{const s=ready(),raw=rawMove(s.revision,{targetNodeId:"old-does-not-exist",nodeProposal:{id:"old-does-not-exist"}}),parsed=api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.throws(()=>api.prepareAiTransaction(parsed),/不存在|出口|节点/)});
test("transition 别名仍要求节点提议",()=>{const s=ready(),raw=rawMove(s.revision,{nodeProposal:null}),parsed=api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.throws(()=>api.prepareAiTransaction(parsed),/地点|节点|提议|目标/)});
console.log(`V155_LOCATION_TRANSITION_ALIAS_TESTS:${passed}:PASS`);
