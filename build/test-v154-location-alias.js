"use strict";
const fs=require("fs"),path=require("path"),vm=require("vm"),assert=require("assert"),{webcrypto}=require("crypto"),{TextEncoder,TextDecoder}=require("util");
const root=path.resolve(__dirname,"..");
const files=["scenarios/library.js","state.js","check-engine.js","scenario-engine.js","memory.js","ai-protocol.js","saves.js"];
function storage(){const map=new Map();return{getItem:k=>map.has(String(k))?map.get(String(k)):null,setItem:(k,v)=>map.set(String(k),String(v)),removeItem:k=>map.delete(String(k)),clear:()=>map.clear(),key:i=>Array.from(map.keys())[i]??null,get length(){return map.size}}}
const localStorage=storage(),sessionStorage=storage();
const sandbox={Object,Array,JSON,Map,Set,console,crypto:webcrypto,TextEncoder,TextDecoder,URL,AbortController,Blob,structuredClone,fetch:async()=>{throw new Error("fetch not expected")},setTimeout:()=>0,clearTimeout(){},setInterval:()=>0,clearInterval(){},window:{localStorage,sessionStorage,addEventListener(){}},document:{querySelector(){return null},querySelectorAll(){return[]},createElement(){return{className:"",textContent:"",style:{},classList:{add(){},remove(){},toggle(){}},appendChild(){},remove(){},click(){}}},body:{appendChild(){}}},confirm:()=>false,renderAll(){},renderTopbar(){},renderSidebar(){},renderChat(){},renderChatLog(){},renderChatComposer(){},renderSaves(){},toast(){}};
sandbox.globalThis=sandbox;
const source=files.map(file=>fs.readFileSync(path.join(root,"src",file),"utf8")).join("\n\n")+`\n;scheduleAutosave=()=>{};renderAll=()=>{};renderTopbar=()=>{};renderSidebar=()=>{};renderChat=()=>{};renderChatLog=()=>{};renderChatComposer=()=>{};renderSaves=()=>{};toast=()=>{};globalThis.__test={SCHEMA_VERSION,scenarioById:id=>deepClone(SCENARIO_LIBRARY.find(x=>x.id===id)),activateScenario,validateAiResponse,prepareAiTransaction,snapshot:()=>deepClone(state),reset:()=>{state=makeInitialState();state.character={system:"coc7",name:"E2E",hp:12,maxHp:12,san:65,maxSan:99,luck:50,attributes:{str:55,con:60,siz:60,dex:65,app:50,int:70,pow:65,edu:70},skills:[{id:"spot_hidden",name:"侦查",value:75}]};}};`;
vm.createContext(sandbox);vm.runInContext(source,sandbox,{filename:"v154-location-runtime.js"});
const api=sandbox.__test;let passed=0;function test(name,fn){fn();passed++;console.log(`PASS ${name}`)}
function ready(){api.reset();api.activateScenario(api.scenarioById("scenario-old-house"));return api.snapshot()}
function rawMove(baseRevision,nodeProposal={id:"old-servant-room"}){return{protocolVersion:"1.3",requestId:"r-move",baseRevision,decision:"no_check",narrative:"你离开大厅，明确进入管家房。",check:null,stateChanges:[],campaignChanges:[],locationEffect:{type:"transition_proposal",targetNodeId:"old-servant-room"},nodeProposal,endingProposal:null,actionSuggestions:[]}}

test("Schema 保持 8",()=>assert.equal(api.SCHEMA_VERSION,8));
test("nodeProposal.id 安全归一为 targetNodeId",()=>{const s=ready(),parsed=api.validateAiResponse(rawMove(s.revision),{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.equal(parsed.nodeProposal.targetNodeId,"old-servant-room")});
test("id 别名形成的合法当前出口可通过完整事务校验",()=>{const s=ready(),parsed=api.validateAiResponse(rawMove(s.revision),{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"}),tx=api.prepareAiTransaction(parsed);assert.equal(tx.proposal.targetNodeId,"old-servant-room")});
test("显式 targetNodeId 优先于 id 别名",()=>{const s=ready(),raw=rawMove(s.revision,{id:"old-study",targetNodeId:"old-servant-room"}),parsed=api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.equal(parsed.nodeProposal.targetNodeId,"old-servant-room")});
test("name 可作为 title 的兼容别名",()=>{const s=ready(),raw=rawMove(s.revision,{id:"old-servant-room",name:"管家房"}),parsed=api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.equal(parsed.nodeProposal.title,"管家房")});
test("未知节点不会因 id 别名获得授权",()=>{const s=ready(),raw=rawMove(s.revision,{id:"old-does-not-exist"});raw.locationEffect.targetNodeId="old-does-not-exist";const parsed=api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.throws(()=>api.prepareAiTransaction(parsed),/不存在|出口|节点/)});
test("空 id 不会从叙事猜测目标",()=>{const s=ready(),raw=rawMove(s.revision,{id:""});const parsed=api.validateAiResponse(raw,{requestId:"r-move",baseRevision:s.revision,stage:"action_adjudication"});assert.throws(()=>api.prepareAiTransaction(parsed),/地点|节点|目标/)});
console.log(`V154_LOCATION_ALIAS_TESTS:${passed}:PASS`);
