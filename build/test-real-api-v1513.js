"use strict";
const fs=require("fs"),path=require("path"),vm=require("vm"),assert=require("assert"),{webcrypto}=require("crypto"),{TextEncoder,TextDecoder}=require("util");
const root=path.resolve(__dirname,"..");
const files=["scenarios/library.js","state.js","check-engine.js","scenario-engine.js","case-integrity.js","memory.js","ai-protocol.js","player-action-guard.js","interaction-availability.js","saves.js","api-response-resilience.js","progress-semantics.js","authored-threat-clock.js","npc-knowledge-boundary.js","ending-resolution-gate.js","coc-resolution-engine.js","coc-consequence-contract.js","failure-forward-cost-engine.js","san-loss-resolution.js","san-loss-window.js","hp-damage-state.js","health-stabilization.js","healing-recovery.js","combat-opposed.js","combat-damage.js","firearms-impaling.js"];
function storage(){const map=new Map();return{getItem:k=>map.has(String(k))?map.get(String(k)):null,setItem:(k,v)=>map.set(String(k),String(v)),removeItem:k=>map.delete(String(k)),clear:()=>map.clear(),key:i=>Array.from(map.keys())[i]??null,get length(){return map.size}}}
const localStorage=storage(),sessionStorage=storage();
function node(){return{className:"",textContent:"",innerHTML:"",value:"",checked:false,disabled:false,style:{},dataset:{},classList:{add(){},remove(){},toggle(){}},appendChild(){},remove(){},click(){},insertAdjacentHTML(){},setAttribute(){},removeAttribute(){},addEventListener(){},querySelector(){return null},querySelectorAll(){return[]},scrollHeight:0,scrollTop:0}}
const sandbox={Object,Array,JSON,Map,Set,console,crypto:webcrypto,TextEncoder,TextDecoder,URL,AbortController,Blob,structuredClone,fetch:globalThis.fetch,setTimeout:globalThis.setTimeout,clearTimeout:globalThis.clearTimeout,setInterval:()=>0,clearInterval(){},window:{localStorage,sessionStorage,addEventListener(){}},document:{addEventListener(){},querySelector(){return null},querySelectorAll(){return[]},createElement:node,body:{appendChild(){}}},navigator:{clipboard:{writeText:async()=>{}}},confirm:()=>false,alert(){},renderAll(){},renderTopbar(){},renderSidebar(){},renderChat(){},renderChatLog(){},renderChatComposer(){},renderSaves(){},toast(){}};
sandbox.globalThis=sandbox;
const source=files.map(file=>fs.readFileSync(path.join(root,"src",file),"utf8")).join("\n\n")+`\n;scheduleAutosave=()=>{};maybeAutoSummarize=()=>{};renderAll=()=>{};renderTopbar=()=>{};renderSidebar=()=>{};renderChat=()=>{};renderChatLog=()=>{};renderChatComposer=()=>{};renderSaves=()=>{};toast=()=>{};
function __character(){return{system:"coc7",name:"Real API Investigator",hp:12,maxHp:12,san:65,maxSan:99,luck:55,attributes:{str:55,con:60,siz:60,dex:65,app:50,int:70,pow:65,edu:70},skills:[{id:"spot_hidden",name:"侦查",value:75},{id:"library_use",name:"图书馆使用",value:70},{id:"psychology",name:"心理学",value:65},{id:"persuade",name:"说服",value:65},{id:"listen",name:"聆听",value:60},{id:"mechanical_repair",name:"机械维修",value:55}]}}
function __scenario(){return{id:"scenario-v1513-real",title:"v1.5.13 真实 API 验收案",mode:"structured",system:"coc7",metadata:{era:"1920s",difficulty:"test",themes:["长局验收"]},briefing:{subtitle:"雨夜失踪案",playerRole:"接受委托的调查员",knownFacts:["暴雨封路","失踪者最后进入宅邸"],caseObjectives:["确认失踪者下落","带回可信证据"],opening:"你抵达宅邸大厅，管家正在等你。"},director:{knowledgeFacts:[{id:"fact-door",text:"书房后存在地下入口。",aliases:["地下入口","书房后的入口"],knownBy:["npc-butler"],learnableFromClueIds:["clue-blueprint"]},{id:"fact-experiment",text:"地下室正在进行非法实验。",aliases:["地下非法实验","非法实验"],knownBy:["npc-captive"],learnableFromClueIds:["clue-notes"]}],threatClocks:[{id:"clock-storm",name:"暴雨封路",current:0,max:3,consequence:"退路恶化",authored:true,maxAdvancePerEvaluation:1,advanceRules:[{id:"stall-pressure",event:"stall",turns:2,amount:1,once:false,cooldownTurns:2}],resolveRules:[{id:"contained",event:"flag",flag:"threatContained",equals:true,once:true}]}]},initialLeads:[{id:"lead-door",text:"调查书房后的入口",status:"active"}],initialQuestions:[{id:"question-captive",text:"失踪者是否仍然活着？",status:"open"}],endings:[{id:"ending-withdraw",title:"调查中止",playerTitle:"雨夜撤离",alwaysAvailable:true,priority:1,summary:"你选择撤离。"},{id:"ending-solved",title:"案件解决",playerTitle:"带着人证与物证离开",requiredFlags:["survivorRescued","threatContained"],requiredClueIds:["clue-ledger","clue-blueprint","clue-notes"],requiredResolvedQuestionIds:["question-captive"],requiredClockStates:[{clockId:"clock-storm",state:"resolved"}],requiredSemanticKinds:["DISCOVERY","ACCESS","SOCIAL","THREAT"],priority:100,summary:"你救出失踪者并带回证据。"}],chapters:[{id:"chapter",title:"调查",scenes:[{id:"scene",title:"宅邸",nodes:[{id:"node-hall",title:"大厅",background:"雨水敲打窗户，管家守在门边。",goals:[],clues:[{id:"clue-ledger",name:"残缺账本",description:"账本记录异常资金流。",hidden:true,acquisitionRoutes:[{id:"ledger-auto",type:"automatic"}]}],npcs:[{id:"npc-butler",name:"管家周铭",description:"戒备，但知道书房后存在入口。",attitude:"戒备"}],optionalChecks:[],mandatoryChecks:[],exits:[{id:"to-study",label:"前往书房",targetNodeId:"node-study",condition:null}],keeperNotes:"管家不知道地下非法实验的细节。"},{id:"node-study",title:"书房",background:"书架有近期移动痕迹，桌上散着建筑资料。",goals:[],clues:[{id:"clue-blueprint",name:"改建图",description:"图纸标出书房后的地下入口。",hidden:true,acquisitionRoutes:[{id:"blueprint-auto",type:"automatic"}]}],npcs:[],optionalChecks:[],mandatoryChecks:[],exits:[{id:"to-cellar",label:"进入地下室",targetNodeId:"node-cellar",condition:null}],keeperNotes:"图纸能够确认入口。"},{id:"node-cellar",title:"地下室",background:"失踪者被困在实验设备旁。",goals:[],clues:[{id:"clue-notes",name:"实验速记",description:"记录地下非法实验的参与者与流程。",hidden:true,acquisitionRoutes:[{id:"notes-auto",type:"automatic"}]}],npcs:[{id:"npc-captive",name:"失踪者沈墨",description:"虚弱，但亲眼见过实验。",attitude:"信任"}],optionalChecks:[],mandatoryChecks:[],exits:[],keeperNotes:"沈墨知道非法实验事实。"}]}]}]}}
function __ready(){state=makeInitialState();state.character=__character();activateScenario(__scenario());state.config.timeoutMs=90000;state.config.temperature=0.2;return deepClone(state)}
function __configure(key,url,model){state.config.apiUrl=url;state.config.model=model;saveApiKey(key,false,{apiUrl:url});return{apiUrl:state.config.apiUrl,model:state.config.model}}
function __commitShape({stateChanges=[],campaignChanges=[],narrative="浏览器验收准备。"}={}){const parsed={protocolVersion:"1.3",requestId:"seed",baseRevision:state.revision,decision:"no_check",narrative,stateChanges:deepClone(stateChanges),campaignChanges:deepClone(campaignChanges),locationEffect:{type:"stay",targetNodeId:null},nodeProposal:null,endingProposal:null,actionSuggestions:[]},tx=prepareAiTransaction(parsed,{});commitAiTransaction(tx,"seed-"+uid("commit"));return deepClone(state)}
function __seedClue(id){const found=findScenarioClue(id);if(!found)throw new Error("missing clue "+id);const route=(found.clue.acquisitionRoutes||[]).find(x=>x.type==="automatic")||found.clue.acquisitionRoutes?.[0];return __commitShape({stateChanges:[{operation:"revealClue",clueId:id,sourceRouteId:route.id}],narrative:"浏览器确认一条已获取线索。"})}
function __enter(id){enterNode(id,{meaningfulProgress:true,reason:"real api e2e browser-owned progression"});return deepClone(state)}
function __resolveQuestion(){return __commitShape({campaignChanges:[{operation:"resolveQuestion",questionId:"question-captive"}],narrative:"浏览器确认失踪者仍然活着。"})}
function __advanceThreat(){const d=state.campaign.directorState;d.sceneTurns=Math.max(Number(d.sceneTurns||0),Number(d.lastProgressTurn||0))+2;d.lastProgressTurn=Math.max(0,d.sceneTurns-2);d.totalTurns=Math.max(Number(d.totalTurns||0)+1,Number(d.authoredClockLastEvaluationTurn??-1)+1);return deepClone(applyDeterministicPacingBeforeAction()||null)}
function __finishFlags(){return __commitShape({stateChanges:[{operation:"setScenarioFlag",flag:"survivorRescued",value:true},{operation:"setScenarioFlag",flag:"threatContained",value:true}],narrative:"浏览器确认救援完成且威胁受控。"})}
function __gate(){return deepClone(endingGateEvaluate(state.scenario.endings.find(x=>x.id==="ending-solved")))}
function __dismissEnding(){if(state.runtime.pendingEndingProposal){state.runtime.pendingEndingProposal=null;setPhase("awaiting_player_action",{force:true});bumpRevision()}return deepClone(state)}
async function __action(text){return await requestPlayerAction(text,{appendPlayerMessage:true})}
async function __roll(){return await resolvePendingCheck()}
function __confirmNode(){confirmNodeProposal();return deepClone(state)}
function __confirmEnding(){return confirmEndingProposal()}
function __dismissRecovery(){if(state.runtime.failedRequest?.recoverable)dismissApiRecovery();return deepClone(state)}
function __state(){return deepClone(state)}
function __diag(){return deepClone(apiReliabilityState())}
globalThis.__test={APP_VERSION,SCHEMA_VERSION,AI_PROTOCOL_VERSION,API_RESPONSE_RESILIENCE_VERSION,COC_RESOLUTION_ENGINE_VERSION,COC_CONSEQUENCE_CONTRACT_VERSION,FAILURE_FORWARD_COST_ENGINE_VERSION,SAN_LOSS_RESOLUTION_VERSION,HP_DAMAGE_STATE_VERSION,HEALTH_STABILIZATION_VERSION,HEALING_RECOVERY_VERSION,COMBAT_OPPOSED_VERSION,COMBAT_DAMAGE_VERSION,FIREARMS_IMPALING_VERSION,ready:__ready,configure:__configure,seedClue:__seedClue,enter:__enter,resolveQuestion:__resolveQuestion,advanceThreat:__advanceThreat,finishFlags:__finishFlags,gate:__gate,dismissEnding:__dismissEnding,action:__action,roll:__roll,confirmNode:__confirmNode,confirmEnding:__confirmEnding,dismissRecovery:__dismissRecovery,state:__state,diag:__diag};`;
vm.createContext(sandbox);vm.runInContext(source,sandbox,{filename:"v1513-real-api-runtime.js"});
const api=sandbox.__test;
const key=String(process.env.TRPG_TEST_API_KEY||"").trim();
const apiUrl=String(process.env.TRPG_TEST_API_URL||"https://api.deepseek.com/v1/chat/completions").trim();
const model=String(process.env.TRPG_TEST_MODEL||"deepseek-v4-flash").trim();
if(!key)throw new Error("TRPG_TEST_API_KEY is required");
const stats={actions:0,checks:0,nodeConfirmations:0,endingProposals:0,endingConfirmed:0,gracefulFallbacksObserved:0,hardFailures:0,technicalLeaks:0};
let scannedMessages=0;
function assertCanonical(){const s=api.state();assert(s.scenario&&s.character);assert(Array.isArray(s.clues)&&Array.isArray(s.npcs));assert.notEqual(s.runtime.phase,"error");assert(Number.isInteger(Number(s.revision)));const clock=s.campaign.directorState.clocks.find(x=>x.id==="clock-storm");assert(clock);assert(clock.current>=0&&clock.current<=clock.max)}
function scanNarrative(){const s=api.state(),messages=(s.messages||[]).slice(scannedMessages);scannedMessages=(s.messages||[]).length;for(const msg of messages.filter(x=>x.role==="ai")){const text=String(msg.content||"");if(/\b(?:node|clue|fact|clock|ending|npc)-[a-z0-9-]+\b/i.test(text)||/\b(?:requestId|baseRevision|trueState|campaignChanges|stateChanges)\b/.test(text)){stats.technicalLeaks++;throw new Error("player-facing narrative leaked internal protocol/backend identifier")}}}
async function settle({confirmFinalEnding=false}={}){for(let i=0;i<10;i++){let s=api.state();if(s.runtime.phase==="error"){stats.hardFailures++;throw new Error("runtime entered error phase: "+String(s.runtime.lastError?.code||s.runtime.lastError?.message||"unknown"))}if(s.runtime.failedRequest?.recoverable){stats.gracefulFallbacksObserved++;api.dismissRecovery();return}if(s.runtime.phase==="awaiting_check"){stats.checks++;await api.roll();continue}if(s.runtime.phase==="awaiting_node_confirmation"){stats.nodeConfirmations++;api.confirmNode();continue}if(s.runtime.phase==="awaiting_ending_confirmation"){stats.endingProposals++;const proposal=s.runtime.pendingEndingProposal;if(confirmFinalEnding&&proposal?.endingId==="ending-solved"&&api.gate().ready){assert.equal(api.confirmEnding(),true);stats.endingConfirmed++;return}api.dismissEnding();return}if(s.runtime.phase==="awaiting_player_action"||s.runtime.phase==="campaign_ended")return;throw new Error("unexpected runtime phase after provider call: "+s.runtime.phase)}throw new Error("settle loop exceeded safety limit")}
async function runAction(text,options={}){const before=api.state().revision,ok=await api.action(text);stats.actions++;if(!ok){const s=api.state();if(s.runtime.failedRequest?.recoverable){stats.gracefulFallbacksObserved++;api.dismissRecovery();assertCanonical();return{graceful:true}}stats.hardFailures++;const err=s.runtime.lastError||{},raw=String(s.runtime.lastRawAiResponse||"").slice(0,5000);console.error("REAL_API_V1513_STRICT_FAILURE:"+JSON.stringify({action:text,phase:s.runtime.phase,code:err.code||s.runtime.failedRequest?.errorCode||null,message:err.message||s.runtime.failedRequest?.errorMessage||null,details:err.details||s.runtime.failedRequest?.errorDetails||null,rawResponse:raw}));throw new Error("player action failed without graceful provider recovery: "+String(err.code||s.runtime.failedRequest?.errorCode||"unknown"))}await settle(options);assert(api.state().revision>=before);assertCanonical();scanNarrative();return{graceful:false}}

(async()=>{
  api.ready();api.configure(key,apiUrl,model);
  assert.equal(api.APP_VERSION,"1.6.10");assert.equal(api.SCHEMA_VERSION,8);assert.equal(api.AI_PROTOCOL_VERSION,"1.3");assert.equal(api.API_RESPONSE_RESILIENCE_VERSION,"1.0");assert.equal(api.COC_RESOLUTION_ENGINE_VERSION,"1.0");assert.equal(api.COC_CONSEQUENCE_CONTRACT_VERSION,"1.0");assert.equal(api.FAILURE_FORWARD_COST_ENGINE_VERSION,"1.0");assert.equal(api.SAN_LOSS_RESOLUTION_VERSION,"1.0");assert.equal(api.HP_DAMAGE_STATE_VERSION,"1.0");assert.equal(api.HEALTH_STABILIZATION_VERSION,"1.0");assert.equal(api.HEALING_RECOVERY_VERSION,"1.0");assert.equal(api.COMBAT_OPPOSED_VERSION,"1.0");assert.equal(api.COMBAT_DAMAGE_VERSION,"1.0");assert.equal(api.FIREARMS_IMPALING_VERSION,"1.0");assert.equal(api.HEALTH_STABILIZATION_VERSION,"1.0");

  await runAction("我问管家周铭：你是否知道地下是否在进行非法实验？如果你不知道，就明确说不知道，不要替你不知道的事作证。");
  await runAction("我再观察一下大厅和管家的反应；如果没有新的可确认信息，就如实告诉我没有新的发现。");

  api.seedClue("clue-ledger");assert(api.state().clues.some(x=>x.id==="clue-ledger"));
  await runAction("我把已经找到的残缺账本给管家看，问他对账本内容有什么反应。只基于他本人知道的内容回答。");

  api.enter("node-study");assert.equal(api.state().campaign.currentNodeId,"node-study");
  await runAction("我检查书房当前能看到的环境和资料。不要替我宣布尚未由系统确认的地下真相。");
  api.seedClue("clue-blueprint");assert(api.state().clues.some(x=>x.id==="clue-blueprint"));
  await runAction("我根据已经找到的改建图，继续确认书房后的地下入口，并把图纸作为已获得证据来讨论。");

  api.advanceThreat();const advanced=api.state().campaign.directorState.clocks.find(x=>x.id==="clock-storm");assert(advanced.current>=1);assert(api.state().campaign.progressSemantics.history.some(x=>(x.kinds||[]).includes("THREAT")));
  api.enter("node-cellar");assert.equal(api.state().campaign.currentNodeId,"node-cellar");
  await runAction("我询问失踪者沈墨：他亲眼见到了什么？只说他本人确实知道的事实，不要让其他 NPC 获得同样知识。");
  api.seedClue("clue-notes");api.resolveQuestion();api.finishFlags();
  const finalClock=api.state().campaign.directorState.clocks.find(x=>x.id==="clock-storm");assert.equal(finalClock.resolved,true);assert.equal(api.gate().ready,true);

  await runAction("我已经救出失踪者并带齐浏览器确认的证据，威胁也已受控。我选择正式结案；如果当前有与这些事实匹配且 ready=true 的结局，请提出该结局供我确认。",{confirmFinalEnding:true});
  if(api.state().runtime.phase!=="campaign_ended"){
    await runAction("我明确选择完成本案并提交当前 ready 的解决型结局，请返回对应 endingProposal 供确认。",{confirmFinalEnding:true});
  }
  const finalState=api.state(),finalGate=api.gate(),reliability=api.diag(),structuredRequests=Number(reliability.structuredRequests||0),gracefulFallbacks=Number(reliability.gracefulFallbacks||0),providerSuccessfulStructuredRequests=Math.max(0,structuredRequests-gracefulFallbacks),ended=finalState.runtime.phase==="campaign_ended"&&finalState.campaign.ending?.id==="ending-solved",providerDeferredEnding=!ended&&finalState.runtime.phase==="awaiting_player_action"&&finalGate.ready===true&&Array.isArray(finalGate.missing)&&finalGate.missing.length===0&&gracefulFallbacks>0&&Number(reliability.hardFailures||0)===0&&stats.hardFailures===0&&stats.technicalLeaks===0&&providerSuccessfulStructuredRequests>=Math.ceil(structuredRequests/2);
  if(!ended&&!providerDeferredEnding){const aiTail=(finalState.messages||[]).filter(x=>x.role==="ai").slice(-4).map(x=>String(x.content||"").slice(0,1200));console.error("REAL_API_V1513_FINAL_DIAGNOSTICS:"+JSON.stringify({phase:finalState.runtime.phase,pendingEndingProposal:finalState.runtime.pendingEndingProposal||null,endingGate:finalGate,failedRequest:finalState.runtime.failedRequest?{kind:finalState.runtime.failedRequest.kind,errorCode:finalState.runtime.failedRequest.errorCode,recoverable:finalState.runtime.failedRequest.recoverable}:null,reliability,providerSuccessfulStructuredRequests,stats,aiTail}))}
  assert(ended||providerDeferredEnding,"真实 provider 必须完成 ending-solved，或在 browser ending gate 已 ready 时通过 bounded graceful fallback 保持可交互");
  assert.equal(stats.hardFailures,0);assert.equal(stats.technicalLeaks,0);assert(stats.actions>=7);assert.equal(Number(reliability.hardFailures||0),0);
  if(ended)assert(stats.endingConfirmed>=1);else{assert.equal(finalGate.ready,true);assert.equal(finalGate.missing.length,0);assert(gracefulFallbacks>0);assert(providerSuccessfulStructuredRequests>=Math.ceil(structuredRequests/2))}
  console.log("REAL_API_V1513_RESULT:"+JSON.stringify({model,actions:stats.actions,checks:stats.checks,nodeConfirmations:stats.nodeConfirmations,endingProposals:stats.endingProposals,endingConfirmed:stats.endingConfirmed,gracefulFallbacksObserved:stats.gracefulFallbacksObserved,technicalLeaks:stats.technicalLeaks,structuredRequests:reliability.structuredRequests,providerSuccessfulStructuredRequests,apiAttempts:reliability.apiAttempts,automaticRetries:reliability.automaticRetries,retryExhausted:reliability.retryExhausted,providerEmpty:reliability.providerEmpty,jsonInvalid:reliability.jsonInvalid,endingDeferredByProvider:providerDeferredEnding,finalPhase:finalState.runtime.phase,endingId:finalState.campaign.ending?.id||null}));
})().catch(error=>{console.error("REAL_API_V1513_FAILURE:"+(error?.stack||error?.message||String(error)));process.exitCode=1});
