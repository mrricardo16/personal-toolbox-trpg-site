# Repository Instructions

- Keep all edited text files in UTF-8. Do not corrupt existing Chinese comments, copy, or logs. If a file appears not to be UTF-8, stop and request confirmation before rewriting it.
- `https://github.com/mrricardo16/personal-toolbox-trpg-site.git` on `main` is the only writable remote for this repository.
- `https://github.com/mrricardo16/personal-toolbox.git` is a read-only legacy/stable source. Do not commit, push, delete, or force-push there.
- Preserve the root-level Single Player structure: `README.md`, `src/`, `build/`, `outputs/`, `reports/`, and `.github/`.
- `outputs/trpg-dm-assistant.html` is the formal release artifact and must remain tracked.
- Do not start Multiplayer implementation unless a later task explicitly authorizes it.
